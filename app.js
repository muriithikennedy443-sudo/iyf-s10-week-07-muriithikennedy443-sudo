// Declare variables properly
const name = 'Kennedy';
let age = 20;

// Use the variables (prevents no-unused-vars)
console.log(name);
console.log(age);

// Example condition (proper syntax)
if (age === 20) {
    console.log('You are 20 years old');
}

// Another example
const total = 50;
console.log(total);